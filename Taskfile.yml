version: '3'

tasks:
  install:local:
    desc: Install the local Nox CLI, runtime, and Codex skill
    cmds:
      - ./install.sh local
  install:remote:
    desc: Install the Linux Nox remote worker without starting it
    cmds:
      - ./install.sh remote
