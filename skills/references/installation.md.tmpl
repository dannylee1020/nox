# Installed Nox CLI

Preferred command: `nox`

Installed binary: `__NOX_BINARY__`

Resolve the command with `command -v nox` first. If it is unavailable on `PATH`, invoke the installed binary directly:

```bash
__NOX_BINARY__ doctor
```

The skill was installed by Nox's installer. Do not replace or edit the installed skill manually; rerun the installer to upgrade it.
