interface:
  display_name: Nox
  short_description: Delegate coding work to an isolated Nox sandbox
  default_prompt: Run this task through Nox and report the result branch.

policy:
  allow_implicit_invocation: false
